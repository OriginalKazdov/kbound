"""FastAPI service layer.

Importing this package requires the optional `[server]` extras
(`pip install kbound[server]`) — fastapi + uvicorn + python-multipart.
The core library (`kbound.recover_rule`, `kbound.check_compliance`,
`kbound.diff_traces`) does NOT require the server extras.
"""
